export type PrimaryValidationServiceConfig = {
    environment: string;
    schema: string;
    prev_start_timestamp: string;
    prev_end_timestamp: string;
    current_start_timestamp: string;
    current_end_timestamp: string;
}